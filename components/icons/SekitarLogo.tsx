
import React from 'react';

const SekitarLogo: React.FC<{ className?: string }> = ({ className }) => (
  <img src="/images/logo-header-sekitar.png" alt="Sekitar Logo" className={className} />
);

export default SekitarLogo;
